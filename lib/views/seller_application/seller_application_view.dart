import 'package:flutter/material.dart';
class SellerApplicationView extends StatelessWidget {
  const SellerApplicationView({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Apply as Seller')));
}

<?xml version="1.0" encoding="utf-8"?>
<!--
  Chokro's durable state is server-backed. Restoring Firebase credentials,
  Firestore caches, or an FCM installation onto another/shared handset can
  restore the wrong person's session or device token, so no private app state
  belongs in Android 11-and-earlier Auto Backup.
-->
<full-backup-content>
    <exclude domain="root" path="."/>
    <exclude domain="file" path="."/>
    <exclude domain="database" path="."/>
    <exclude domain="sharedpref" path="."/>
    <exclude domain="external" path="."/>
    <exclude domain="device_root" path="."/>
    <exclude domain="device_file" path="."/>
    <exclude domain="device_database" path="."/>
    <exclude domain="device_sharedpref" path="."/>
</full-backup-content>
